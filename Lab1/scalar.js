// Function declaration that increments a number by 1
function inc(n) {
    return n + 1;
}

// Declaration of variables
const a = 1;
const b = inc(a);

// Display details of all properties of the transferred object
console.dir({ a, b });